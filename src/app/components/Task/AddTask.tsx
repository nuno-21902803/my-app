import React from 'react';

const AddTask = () => {
    return (
        <div>
            <button className="btn btn-primary w-full">
                    Add new Task
            </button>
        </div>
    );
};

export default AddTask;